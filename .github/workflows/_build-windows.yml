name: Build Windows Image Template
on:
  workflow_call:
    inputs:
      tags:
        type: string
        required: false
        description: "Extra tags to use besides main tag."

jobs:
  build:
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v2
      - name: Docker Login
        run: echo ${{ secrets.DOCKER_ACCESS_TOKEN }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
      - name: Build Image
        run: docker build -t steamcmd/steamcmd:${{ github.job }}.
        working-directory: dockerfiles/${{ github.job }}
      - name: Test Image
        run: docker run steamcmd/steamcmd:${{ github.job }} +quit

      # master
      - name: Tag Image
        run: >
          for %TAG in (${{ inputs.tags }})
          do
            docker tag steamcmd/steamcmd:${{ inputs.name }} steamcmd/steamcmd:%TAG
        if: ${{ (github.ref == 'refs/heads/master') && (inputs.tags != '') }}
      - name: Push Image
        run: docker push steamcmd/steamcmd --all-tags
        if: ${{ github.ref == 'refs/heads/master' }}